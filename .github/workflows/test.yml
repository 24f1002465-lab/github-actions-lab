name: Test Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f1002465@ds.study.iitm.ac.in
        run: echo "Hello, world!"
